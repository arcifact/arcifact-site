# The analyser paths are served by a WORKER ROUTE on the zone, not from
# here. Cloudflare Pages _redirects can only rewrite same-origin paths
# at status 200; pointing one at another host silently 404s, which is
# what it did. Recorded so the next person does not retry it.
